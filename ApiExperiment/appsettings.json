{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // may add an api key authentication in the future

  // used for the public url. Do NOT include a trailing / as that will mess up a few urls.
  // can use localhost:7189 to debug/test during VisualStudio debugging.
  "Settings": {
    "PublicUrl": "https://api.cronotech.us",
    "SupportEmail": "support@cronotech.us"
  }
}
